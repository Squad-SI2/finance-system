export type CreateUserRequest = {
  email: string;
  password: string;
  firstName: string;
  lastName: string;
};

export type UserFormValue = {
  email: string;
  password: string;
  firstName: string;
  lastName: string;
};
