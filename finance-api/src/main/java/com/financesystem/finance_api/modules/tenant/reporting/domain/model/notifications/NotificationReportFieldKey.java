package com.financesystem.finance_api.modules.tenant.reporting.domain.model.notifications;

public enum NotificationReportFieldKey {
    NOTIFICATION_ID,
    USER_ID,
    TYPE,
    CATEGORY,
    PRIORITY,
    TITLE,
    BODY,
    DATA,
    IMAGE_URL,
    ACTION_URL,
    READ_AT,
    OPENED_AT,
    ARCHIVED_AT,
    EXPIRES_AT,
    CREATED_AT,
    UPDATED_AT,
    IS_READ,
    IS_OPENED,
    IS_ARCHIVED,
    IS_EXPIRED
}
