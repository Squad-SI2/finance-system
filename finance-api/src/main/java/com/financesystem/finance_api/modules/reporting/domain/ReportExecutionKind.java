package com.financesystem.finance_api.modules.reporting.domain;

/** Whether an execution came from a controlled definition or the AI rail. */
public enum ReportExecutionKind {
    CONTROLLED,
    AI
}
