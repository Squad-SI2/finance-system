package com.financesystem.finance_api.modules.tenant.reporting.domain.model.notificationdevices;

public enum NotificationDeviceReportFieldKey {
    DEVICE_RECORD_ID,
    USER_ID,
    DEVICE_ID,
    PLATFORM,
    DEVICE_NAME,
    APP_VERSION,
    OS_VERSION,
    STATUS,
    LAST_SEEN_AT,
    CREATED_AT,
    UPDATED_AT
}
